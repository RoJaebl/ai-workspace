# 마이그레이션 분석 가이드

## 목차
1. [분석 체크리스트](#분석-체크리스트)
2. [의존성 버전 분석](#의존성-버전-분석)
3. [Breaking Changes 탐지](#breaking-changes-탐지)
4. [마이그레이션 전략](#마이그레이션-전략)

## 분석 체크리스트

### 의존성 현황 파악

```bash
# Node.js
npm outdated
npm audit

# Python
pip list --outdated
pip-audit

# 버전 잠금 파일 확인
cat package-lock.json | jq '.packages | keys'
```

### 주요 확인 항목

- [ ] 주요 프레임워크 버전 (React, Next.js, NestJS 등)
- [ ] 언어 버전 (Node.js, Python, TypeScript)
- [ ] 보안 취약점이 있는 패키지
- [ ] Deprecated 패키지
- [ ] 라이선스 호환성

## 의존성 버전 분석

### Semantic Versioning 이해

```
MAJOR.MINOR.PATCH
  │     │     └── 버그 수정 (호환)
  │     └──────── 기능 추가 (호환)
  └────────────── Breaking Changes (비호환)
```

### 업그레이드 우선순위

| 우선순위 | 조건 | 예시 |
|----------|------|------|
| 🔴 즉시 | 보안 취약점 | CVE가 있는 패키지 |
| 🟠 높음 | MAJOR 버전 뒤처짐 2+ | React 16 → 18 |
| 🟡 중간 | MINOR 버전 뒤처짐 | 새 기능 필요 시 |
| 🟢 낮음 | PATCH만 뒤처짐 | 안정성 확인 후 |

## Breaking Changes 탐지

### TypeScript/JavaScript

```bash
# Deprecated API 사용 탐지
grep -r "deprecated" node_modules/@types/ 2>/dev/null | head -10

# 마이그레이션 가이드 참조
# - React: https://react.dev/blog
# - Next.js: https://nextjs.org/docs/upgrading
```

### 일반적인 Breaking Changes

| 프레임워크 | 버전 | 주요 변경 |
|------------|------|----------|
| **React** | 17→18 | Concurrent Mode, useId, Suspense |
| **Next.js** | 13→14 | App Router, Server Components |
| **TypeScript** | 4→5 | Decorator, const Type Parameters |
| **Node.js** | 18→20 | ESM 기본, fetch 내장 |

## 마이그레이션 전략

### 점진적 마이그레이션

```
Phase 1: 준비
├── 현재 상태 백업
├── 테스트 커버리지 확보
└── CI/CD 파이프라인 준비

Phase 2: 의존성 업그레이드
├── Patch 버전 먼저
├── Minor 버전 다음
└── Major 버전 마지막

Phase 3: 코드 수정
├── Breaking Changes 대응
├── Deprecated API 교체
└── 새 API 적용

Phase 4: 검증
├── 전체 테스트 실행
├── 성능 벤치마크
└── 스테이징 배포
```

### Codemods 활용

```bash
# React codemod
npx @next/codemod upgrade latest

# ESLint codemod
npx @eslint/migrate-config .eslintrc.json
```

## 롤백 계획

### 롤백 체크리스트

- [ ] 이전 버전 package-lock.json 백업
- [ ] 데이터베이스 마이그레이션 롤백 스크립트
- [ ] 배포 롤백 절차 문서화
- [ ] 롤백 테스트 완료

### 롤백 명령어

```bash
# Node.js 롤백
git checkout HEAD~1 -- package.json package-lock.json
npm ci

# 데이터베이스 롤백
npm run migration:rollback
```

## 위험 평가 매트릭스

| 위험 요소 | 영향도 | 발생 가능성 | 완화 방법 |
|-----------|--------|------------|----------|
| API 호환성 깨짐 | 높음 | 중간 | 단계적 마이그레이션 |
| 성능 저하 | 중간 | 낮음 | 벤치마크 테스트 |
| 테스트 실패 | 중간 | 높음 | 테스트 커버리지 확보 |
| 배포 실패 | 높음 | 낮음 | 블루-그린 배포 |
