---
name: moc-indexing
description: MOC(Map of Content) 시스템의 파일/폴더 생성, 인덱싱, 메타데이터 관리를 수행합니다. 사용 시점: (1) MOC 폴더 내 새 노트/폴더 생성, (2) 인덱스 부여 및 이모지 선택, (3) YAML frontmatter 작성, (4) 폴더-마스터노트 동기화, (5) 계층 승격/이동 및 리팩토링, (6) 무결성 검사가 필요할 때.
---

# MOC 인덱싱 스킬

## 핵심 구조

### 계층별 이모지 및 인덱스

| Level | 이모지 | 인덱스 패턴 | 예시 |
|-------|--------|-------------|------|
| 1 | `🏛` | 숫자 | `1` |
| 2 | `📚` | L1.숫자 | `1.1` |
| 3 | `📔` | L2+알파벳 | `1.1a` |
| 4 | 가변 | L3.숫자 | `1.1a.1` |
| 5 | 가변 | L4+알파벳 (권장 최대) | `1.1a.1a` |
| 6+ | 가변 | L5+숫자 | `1.1a.1a1` |

> **Level 4+ 의도적 설계**: 회고록, 일지 등 시계열 데이터는 `🌅(연)` → `📅(월)` → `🖋(일)` 순으로 계층적 의미를 전달합니다. 단순한 깊이 증가가 아닌 **지식 성격에 따른 논리적 분기**입니다.

### 파일명 형식

```
{이모지} {인덱스} {이름}.md
```

### YAML Frontmatter

```yaml
---
title: "{이모지} {인덱스} {이름}"
moc id: "{인덱스}"
back link: "[[{부모 마스터노트명}]]"
createdAt: YYYY-MM-DD HH:mm
updatedAt: YYYY-MM-DD HH:mm
---
```

### Back Link 규칙

| 현재 Level | Back Link 대상 |
|------------|----------------|
| Level 3 | **항상** Level 2 (Topic MOC) |
| Level 4+ | 바로 위 상위 노트 |

예: `📔 1.1a 회고록` → `[[📚 1.1 사고]]`

### 원초적 파일 (Atomic Files)

`📘 가이드`, `🧩 모듈`, `🪷 비전` 등 **단일 파일로 완결성을 가진 파일**은 MOC 인덱싱에 포함하지 않습니다. 제목 심볼만으로 성격을 식별합니다.

## 작업 절차

### 새 파일 생성

1. 부모 폴더 인덱스 확인
2. 형제 노드 탐색 → 중복 없는 인덱스 결정
3. 계층/성격/시간 기준으로 이모지 선택
4. 파일 생성 및 YAML 작성

### 폴더 생성 (Level 2-5)

폴더와 마스터노트는 **한 쌍**:
- 폴더명 = 마스터노트명
- 마스터노트는 폴더 **내부**에 위치

```
📔 1.1a 회고록/
└── 📔 1.1a 회고록.md  ← 마스터노트
```

### 승격/이동

1. 물리적 이동
2. 인덱스 재정의
3. YAML 일괄 업데이트 (`title`, `moc id`, `back link`)
4. 내부 위키링크 업데이트

## 무결성 검사

- [ ] 파일 경로 ↔ `moc id` 일치
- [ ] `back link` → 실제 부모 노트
- [ ] 폴더명 = 마스터노트명
- [ ] 시계열 이모지 흐름 유지 (`🌅` → `📅` → `🖋`)

## 에이전트 행동 지침

1. **승격/이동 시**: 폴더 내 모든 파일의 이름, YAML, 본문 헤더, 링크를 새 인덱스에 맞춰 일괄 수정
2. **이모지 일관성**: 단순 노트(`📄`)를 MOC 폴더로 승격 시 → `📔`로 변경
3. **시계열 보존**: 계층 이동 시에도 `🌅`/`📅`/`🖋`의 시간적 속성 유지
4. **⚠️ 새로운 구조 발견 시**: 가이드에 정의되지 않은 이모지 패턴이나 구조 발견 시, **임의 수정하지 말고 사용자에게 보고**하여 가이드 업데이트 여부 확인

## 이모지 선택 플로우

```
1. MOC 계층? → 🏛(L1) / 📚(L2) / 📔(L3)
2. 시계열?   → 🌅(연) / 📅(월) / 🖋(일)
3. 특수 성격? → 🪷(비전) / 📘(가이드) / 🧩(모듈) / 🏠(홈)
4. 그 외    → 📄(일반 문서)
```

## 참조

- **이모지 사전, 인덱스 패턴 상세, 예시 트리**: [reference.md](reference.md)
- **원본 가이드**: `vaults/MOC/AGENTS.md`
